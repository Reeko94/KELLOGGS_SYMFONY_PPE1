{% extends 'base.html.twig' %}

{% block title %}Edit Fabricants{% endblock %}

{% block body %}
    <h1>modifier un fabricant</h1>
    <div class="container">
        {{ form_start(form) }}
        {{ form_row(form.TypeNutrition) }}
        {{ form_row(form.libelle) }}
        {{ form_row(form.logo) }}
        <input type="submit" class="btn btn-success mb-3 btn-block">
        {{ form_end(form) }}
        <div class="row form-group m-0">

        <a class="btn btn-success mr-1" href="{{ path('fabricants_index') }}">retour à la liste</a>
            {{ include('fabricants/_delete_form.html.twig') }}

        </div>
    </div>
{% endblock %}
