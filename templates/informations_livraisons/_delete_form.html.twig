<form method="post" action="{{ path('informations_livraisons_delete', {'id': informations_livraison.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ informations_livraison.id) }}">
    <button class="btn">Delete</button>
</form>
