{% extends 'base.html.twig' %}

{% block title %}TypeNutrition{% endblock %}

{% block body %}
    <h1>TypeNutrition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_nutrition.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_nutrition.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_nutrition_index') }}">back to list</a>

    <a href="{{ path('type_nutrition_edit', {'id': type_nutrition.id}) }}">edit</a>

    {{ include('type_nutrition/_delete_form.html.twig') }}
{% endblock %}
