{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <h1 class="text-center">Nouveautés sur le site !</h1>
    <div class="container mt-4 mb-4">
        <div class="row">
            {% for f in lastfive %}
                <div class="col-md-4">
                    <div class="card mb-3" style="width: 18rem;padding:5px 20px 0 20px;">
                        <div class="card-body">
                            <img class="card-img-top" src="/uploads/article/{{ f.urlMedia }}" height="200" width="100" alt="Card image cap">
                            <h5 class="card-title"><a class="btn btn-success btn-lg btn-block" href="{{ path('fabricants_show',{'id':f.fabricant.id} ) }}">Voir</a></h5>

                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}
